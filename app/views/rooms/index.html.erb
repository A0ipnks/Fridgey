<div class="container mx-auto px-4 py-8">
  <div class="flex flex-col items-center justify-between mb-8 sm:flex-row">
    <h1 class="text-3xl mb-4 font-bold text-gray-800 sm:mb-0">冷蔵庫一覧</h1>
    <div class="space-x-4 flex">
      <%= link_to "新しい冷蔵庫を作成", new_room_path,
          class: "bg-green-600 text-white px-3 py-3 rounded-lg font-semibold hover:bg-green-700 transition-colors" %>
      <%= link_to "招待コードで参加", join_rooms_path,
          class: "bg-blue-600 text-white px-3 py-3 rounded-lg font-semibold hover:bg-blue-700 transition-colors" %>
    </div>
  </div>

  <% if @rooms.any? %>
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @rooms.each do |room| %>
        <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow">
          <div class="flex justify-between items-start mb-4">
            <h3 class="text-xl font-semibold text-gray-800"><%= room.name %></h3>
            <% if current_user.admin_of?(room) %>
              <span class="bg-red-100 text-red-800 text-xs px-2 py-1 rounded-full">管理者</span>
            <% else %>
              <span class="bg-blue-100 text-blue-800 text-xs px-2 py-1 rounded-full">メンバー</span>
            <% end %>
          </div>

          <% if room.description.present? %>
            <p class="text-gray-600 mb-4 text-sm"><%= truncate(room.description, length: 100) %></p>
          <% end %>

          <div class="flex justify-between items-center text-sm text-gray-500 mb-4">
            <span>メンバー数: <%= room.users.count %>人</span>
            <span>作成者: <%= room.created_by.display_name || room.created_by.email %></span>
          </div>

          <div>
            <%= link_to "詳細を見る", room, class: "block w-full text-center bg-blue-500 text-white py-2 rounded hover:bg-blue-600 transition-colors" %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="text-center py-16">
      <div class="text-6xl mb-4">🍃</div>
      <h3 class="text-xl font-semibold text-gray-600 mb-4">まだ冷蔵庫がありません</h3>
      <p class="text-gray-500 mb-8 text-left">新しい冷蔵庫を作成するか、招待コードで既存の冷蔵庫に参加してください。</p>
    </div>
  <% end %>
</div>
