<div class="container mx-auto px-4 py-8 max-w-2xl">
  <h1 class="text-3xl font-bold text-gray-800 mb-8">招待コードで参加</h1>

  <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-8">
    <h3 class="text-lg font-semibold text-blue-800 mb-2">📝 参加手順</h3>
    <ol class="list-decimal list-inside text-blue-700 space-y-1">
      <li>冷蔵庫の管理者から招待コードを教えてもらいましょう</li>
      <li>下のフォームに8桁のコードを入力してください</li>
      <li>「参加する」ボタンをクリックして完了です</li>
    </ol>
  </div>

  <%= form_with url: join_by_code_rooms_path, method: :post, local: true, class: "space-y-6" do |form| %>
    <div>
      <%= form.label :invitation_code, "招待コード", class: "block text-sm font-medium text-gray-700 mb-2" %>
      <%= form.text_field :invitation_code, class: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 text-center text-lg tracking-widest", placeholder: "ABCD1234", maxlength: 8, style: "text-transform: uppercase;" %>
      <p class="text-sm text-gray-500 mt-1">アルファベットと数字を8文字で入力してください</p>
    </div>

    <div class="flex space-x-4">
      <%= form.submit "参加する", class: "bg-blue-600 text-white px-6 py-3 rounded-lg font-semibold hover:bg-blue-700 transition-colors" %>
      <%= link_to "キャンセル", rooms_path, class: "bg-gray-500 text-white px-6 py-3 rounded-lg font-semibold hover:bg-gray-600 transition-colors" %>
    </div>
  <% end %>
</div>
